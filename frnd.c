#include<stdio.h>
#include<conio.h>
void main()
{
int n,i,sum=0;
  printf("enter a value");
  scanf("%d",&n);
        for(i=1;i<=n;++i)
        {
          sum+=i;
        }
        printf("sum %d",sum);
        return 0
        getch();
        }
